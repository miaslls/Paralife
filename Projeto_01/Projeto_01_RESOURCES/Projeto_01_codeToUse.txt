const prompt = require("prompt-sync")();

+ senha.trim().toUpperCase()

---

VALIDAÇÃO:

while (true) {
    VAR = prompt();
    if (VALIDATION - data IN RANGE) {
        break;
    }
    console.log(ERROR);
}